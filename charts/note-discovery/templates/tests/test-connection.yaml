apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "note-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "note-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: notediscovery
      image: ghcr.io/gamosoft/notediscovery
      command: ['sleep', '1']
      args: ['{{ include "note-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
